def call() {
    echo "=== [Shared Lib] Building the application ==="

    // For now we’ll just print.
    // Later, you can replace this with: sh 'mvn clean package' or npm build.
    echo "Pretend: mvn clean package or npm run build"
}
